# v0.9.0 Template Redesign Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Redesign all 6 signature templates with Verdana font, 3-tier content hierarchy, and Classic as default.

**Architecture:** Rewrite template generation functions in `js/signature.js` to match design spec. Update HTML template descriptions. No new files needed.

**Tech Stack:** Vanilla JavaScript (ES6+), HTML5, table-based layouts with inline styles

**Design Reference:** `docs/plans/2026-01-24-v0.9.0-template-redesign-design.md`

---

## Task 1: Update Classic Template (Default)

**Files:**
- Modify: `js/signature.js:204-254` (generateClassicStyle function)

**Changes:**
1. Change font from `Arial, Helvetica, sans-serif` to `Verdana, Geneva, sans-serif`
2. Update logo size from 32px to 34px
3. Adjust name size from 18px to 15px
4. Add 3-tier contact structure (phone/email, personal links, Zoho social)
5. Update spacing (10px logo-to-name, 7px between sections)

**Test:**
- Open `index.html`, select Classic style
- Verify Verdana font renders
- Verify 3-tier structure shows correctly
- Test with all optional fields on/off

**Commit:** `feat: redesign Classic template with Verdana font and 3-tier hierarchy`

---

## Task 2: Update Professional Template

**Files:**
- Modify: `js/signature.js` (generateProfessionalStyle function - find via search)

**Changes:**
1. Change font to Verdana
2. Update logo size to 38px
3. Change name size to 15px
4. Implement 3-tier contact structure
5. Adjust column widths (logo: 75px, info: 420px)

**Test:**
- Select Professional style
- Verify two-column layout renders correctly
- Test dark mode toggle

**Commit:** `feat: redesign Professional template with Verdana and new hierarchy`

---

## Task 3: Update Minimalist Template

**Files:**
- Modify: `js/signature.js` (generateMinimalStyle function)

**Changes:**
1. Change font to Verdana
2. Update name size to 16px (largest of all templates)
3. Remove logo completely (text-only)
4. Implement 3-tier contact structure
5. Add tighter spacing (8px vs 10px between sections)

**Test:**
- Select Minimalist style
- Verify NO logo appears
- Verify text-only layout

**Commit:** `feat: redesign Minimalist template as text-only with Verdana`

---

## Task 4: Update Compact Template

**Files:**
- Modify: `js/signature.js` (generateCompactStyle function)

**Changes:**
1. Change font to Verdana
2. Update logo size to 26px (smallest)
3. Restructure to multi-line (not single-line)
4. Implement 3-tier contact structure in stacked format

**Test:**
- Select Compact style
- Verify small logo (26px)
- Verify multi-line stacked layout

**Commit:** `feat: redesign Compact template with multi-line stacked layout`

---

## Task 5: Update Modern Template

**Files:**
- Modify: `js/signature.js` (generateModernStyle function)

**Changes:**
1. Change font to Verdana
2. Update logo size to 38px
3. Adjust vertical accent bar opacity from 25% to 30%
4. Implement 3-tier contact structure
5. Ensure 2px vertical bar separator

**Test:**
- Select Modern style
- Verify vertical accent bar appears
- Test all 4 accent colors (bar should change color)

**Commit:** `feat: redesign Modern template with 30% opacity accent bar`

---

## Task 6: Update Creative Template

**Files:**
- Modify: `js/signature.js` (generateCreativeStyle function)

**Changes:**
1. Change font to Verdana
2. Update logo size to 32px
3. Reduce left bar width from 5px to 3-4px
4. Implement 3-tier contact structure
5. Logo above name (stacked within bar column)

**Test:**
- Select Creative style
- Verify thick left accent bar (3-4px)
- Verify logo appears above name

**Commit:** `feat: redesign Creative template with thinner left accent bar`

---

## Task 7: Update HTML Template Descriptions

**Files:**
- Modify: `index.html` (style selector section - search for "Classic" or "Professional")

**Changes:**
Update descriptions to user-focused (not design-focused):

```html
<option value="classic">Classic (Default) - Best for everyone</option>
<option value="professional">Professional - Best for Sales, Account Management</option>
<option value="minimalist">Minimalist - Best for Engineering, Technical Support</option>
<option value="compact">Compact - Best for mobile-heavy users</option>
<option value="modern">Modern - Best for Product, Engineering Leadership</option>
<option value="creative">Creative - Best for Marketing, Design, Events</option>
```

**Test:**
- Open dropdown, verify new descriptions
- Verify Classic is first/default

**Commit:** `feat: update template descriptions to be user-focused`

---

## Task 8: Change Default Template to Classic

**Files:**
- Modify: `index.html` (style selector `<select>` element)
- Modify: `js/app.js` (initial state setup - search for "signatureStyle")

**Changes:**
1. Set Classic as selected option in HTML: `<option value="classic" selected>`
2. Update AppState default: `signatureStyle: 'classic'`

**Test:**
- Refresh page
- Verify Classic style loads by default (not Professional)

**Commit:** `feat: set Classic as default template`

---

## Task 9: Add Personal X Field (Optional)

**Files:**
- Modify: `index.html` (add X toggle and input after LinkedIn)
- Modify: `js/app.js` (add X field handling)
- Modify: `js/signature.js` (update contact generation to include X)

**Changes:**
1. Clone LinkedIn field HTML, change to X
2. Add event listeners for X toggle/input
3. Update contact tier 2 to include X link

**Test:**
- Toggle X field on/off
- Enter X handle (without @)
- Verify appears in signature as `x.com/handle`

**Commit:** `feat: add optional personal X field to signatures`

---

## Task 10: Final Testing & Documentation

**Files:**
- Modify: `README.md` (update screenshots, features list)
- Modify: `CHANGELOG.md` (add v0.9.0 section)

**Testing Checklist:**
- [ ] All 6 templates render with Verdana font
- [ ] Classic is default
- [ ] 3-tier hierarchy shows in all templates
- [ ] All 4 accent colors work
- [ ] Dark mode toggles correctly
- [ ] Optional fields (LinkedIn, X, Bookings, Zoho social) work
- [ ] Copy signature button works
- [ ] No JavaScript console errors

**Commit:** `docs: update README and CHANGELOG for v0.9.0 release`

---

## Success Criteria

- ✅ All templates use Verdana font
- ✅ Classic is default template
- ✅ 3-tier content hierarchy implemented
- ✅ Personal X field works
- ✅ User-focused descriptions
- ✅ No regressions (dark mode, accent colors, copy button)

---

## Notes

- **Font change is CRITICAL**: Search/replace ALL instances of `Arial, Helvetica` with `Verdana, Geneva`
- **3-tier structure**: Phone/Email → LinkedIn/X/Bookings → Follow Zoho
- **Logo sizes**: Classic 34px, Professional/Modern 38px, Minimalist none, Compact 26px, Creative 32px
- **Test in browsers**: Chrome, Firefox, Safari minimum
- **Email client testing**: Defer to post-deployment (not blocking)

---

## Execution Strategy

**Batch execution recommended:**
- Batch 1: Tasks 1-3 (Classic, Professional, Minimalist)
- Batch 2: Tasks 4-6 (Compact, Modern, Creative)
- Batch 3: Tasks 7-10 (HTML updates, testing, docs)

**Estimated time:** 2-3 hours total
